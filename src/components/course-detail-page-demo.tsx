"use client";

import { CourseDetailPage } from "@/components/ui/course-detail-page";

export function CourseDetailPageDemo({ className }: { className?: string }) {
  return <CourseDetailPage className={className} />;
}
