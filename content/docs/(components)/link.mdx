---
title: Link
description: Text link component with primary and secondary variants.
base: base
component: true
---

import { AppLink } from "@/components/ui/link";
import { Preview } from "@/components/preview";
import { ArrowRight, Github } from "lucide-react";
import Installation from "@/components/installation";
import registry from "@/registry.json";

## Example

<ComponentPreview name="link">
  <AppLink href="#example">Visit Docs</AppLink>
</ComponentPreview>

## Installation

<Installation code={registry.link.source} fileName="/components/ui/link.tsx" />

## Usage

```tsx
import { AppLink } from "@/components/ui/link";
```

```tsx
<AppLink href="/docs">Doc Link</AppLink>
```

## Examples

### Primary

<Preview>
  <div className="space-x-2 space-y-2">
    <AppLink href="#primary">Primary link</AppLink>
    <AppLink href="#primary" disabled>
      Disabled primary
    </AppLink>
  </div>
</Preview>

### Secondary

<Preview>
  <div className="space-x-2 space-y-2">
    <AppLink href="#secondary" variant="secondary">
      Secondary link
    </AppLink>
    <AppLink href="#secondary" variant="secondary" disabled="true">
      Disabled secondary
    </AppLink>
  </div>
</Preview>

### Affordance icon (`icon`)

By default, a trailing `ExternalLink` icon (same as in these docs) fades in on hover and focus. Use `icon="plus"` for a leading `Plus` icon, or `icon="none"` when you supply your own icons.

<Preview>
  <div className="flex flex-col gap-4">
    <div className="flex flex-wrap items-center gap-4">
      <AppLink href="#default-external">Default</AppLink>
      <AppLink href="#plus-before" icon="plus">
        Plus before
      </AppLink>
      <AppLink href="#no-built-in" icon="none">
        No built-in icon
      </AppLink>
    </div>
  </div>
</Preview>

### With Icon

Compose your own icons with `icon="none"` so the built-in affordance does not duplicate them.

<Preview>
  <div className="flex flex-col space-y-4">
    <div className="flex items-center space-x-4">
      <AppLink href="#icon-leading" icon="plus">
        <Github className="size-4" />
        GitHub
      </AppLink>
      <AppLink href="#icon-trailing" variant="secondary" icon="plus">
        Read More
        <ArrowRight className="size-4" />
      </AppLink>
    </div>
    <div className="flex items-center space-x-4">
      <AppLink href="https://example.com" target="_blank" rel="noreferrer">
        External
      </AppLink>
    </div>
  </div>
</Preview>

## API Reference

### AppLink

| Prop       | Type                             | Description                                                                 |
| ---------- | -------------------------------- | --------------------------------------------------------------------------- |
| `variant`  | `"primary" \| "secondary"`       | Controls underline and color styles.                                        |
| `icon`     | `"external" \| "plus" \| "none"` | Hover/focus affordance: trailing external (default), leading plus, or none. |
| `disabled` | `boolean`                        | Disables interaction and focus.                                             |
| `href`     | `string`                         | Destination URL.                                                            |
