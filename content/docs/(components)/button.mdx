---
title: Button
description: Displays a button or a component that looks like a button.
base: base
component: true
---

import { Button } from "@/components/ui/button";
import { Preview } from "@/components/preview";
import { ButtonVariantShowcase } from "@/components/button-variant-showcase";
import { PropsTable } from "@/components/props-table";
import { ArrowRight, Mail, Loader2 } from "lucide-react";
import Installation from "@/components/installation";
import registry from "@/registry.json";

## Example

<ComponentPreview name="button">
  <Button>Click me</Button>
</ComponentPreview>

## Installation

<Installation
  dependencies={["class-variance-authority"]}
  useReactSlot
  code={registry.button.source}
  fileName="/components/ui/button.tsx"
/>

## Usage

```tsx
import { Button } from "@/components/ui/button";
```

```tsx
<Button variant="primary | secondary | destructive" size="sm | md | lg">
  Button
</Button>
```

## Variants

The button has 3 variants and 3 sizes. Each variant is shown below with all sizes (left to right) and the default, disabled, hover, and focus states.

### Primary

<ButtonVariantShowcase variant="primary" title="Primary" />

### Secondary

<ButtonVariantShowcase variant="secondary" title="Secondary" />

### Destructive

<ButtonVariantShowcase variant="destructive" title="Destructive" />

## Examples

### With Icons

Buttons handle inline icons automatically. The icon spacing is managed by the built-in `gap` styles.

<Preview>
  <div className="flex flex-wrap items-center gap-2 self-start">
    <Button>
      <Mail />
      Email
    </Button>
    <Button variant="secondary">
      Continue
      <ArrowRight />
    </Button>
  </div>
</Preview>

### Loading

You can render a spinner icon to represent a loading state.

<Preview>
  <div className="flex flex-wrap items-center gap-2">
    <Button disabled>
      <Loader2 className="animate-spin" />
      Saving
    </Button>
    <Button variant="secondary" disabled>
      <Loader2 className="animate-spin" />
      Submitting
    </Button>
  </div>
</Preview>

## API Reference

### Button

The `Button` component displays a button or a component that looks like a button.

<PropsTable component="Button" />
