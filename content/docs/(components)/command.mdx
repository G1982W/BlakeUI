---
title: Command
description: Fast command palette with search and keyboard navigation.
base: base
component: true
---

import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
} from "@/components/ui/command";
import { Preview } from "@/components/preview";
import Installation from "@/components/installation";
import registry from "@/registry.json";
import {
  CommandBasicDemo,
  CommandGroupsDemo,
  CommandRtlDemo,
  CommandScrollableDemo,
  CommandShortcutsDemo,
} from "@/components/ui/command-demo";

## Example

<ComponentPreview name="command">
  <Command className="max-w-sm rounded-lg border shadow-md">
    <CommandInput placeholder="Type a command or search..." />
    <CommandList>
      <CommandEmpty>No results found.</CommandEmpty>
      <CommandGroup heading="Suggestions">
        <CommandItem>Calendar</CommandItem>
        <CommandItem>Search Emoji</CommandItem>
        <CommandItem>Calculator</CommandItem>
      </CommandGroup>
      <CommandSeparator />
      <CommandGroup heading="Settings">
        <CommandItem>Profile</CommandItem>
        <CommandItem>Billing</CommandItem>
        <CommandItem>Settings</CommandItem>
      </CommandGroup>
    </CommandList>
  </Command>
</ComponentPreview>

## About

The `Command` component is built on [cmdk](https://cmdk.pacocoursey.com/) by Dip. Use it for searchable menus and command palettes (inline or inside `CommandDialog`).

## Installation

Command uses the Dialog component. Add both `dialog.tsx` and `command.tsx`.

<Installation
  dependencies={["cmdk", "lucide-react"]}
  code={registry.command?.source ?? ""}
  fileName="/components/ui/command.tsx"
/>

## Usage

```tsx
import {
  Command,
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
  CommandShortcut,
} from "@/components/ui/command";
```

**Inline command (popover, card, or sidebar):**

```tsx
<Command className="max-w-sm rounded-lg border">
  <CommandInput placeholder="Type a command or search..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Suggestions">
      <CommandItem>Calendar</CommandItem>
      <CommandItem>Search Emoji</CommandItem>
      <CommandItem>Calculator</CommandItem>
    </CommandGroup>
    <CommandSeparator />
    <CommandGroup heading="Settings">
      <CommandItem>Profile</CommandItem>
      <CommandItem>Billing</CommandItem>
      <CommandItem>Settings</CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

**Command dialog (palette):** wrap the same structure in `CommandDialog` and control `open` / `onOpenChange`. You can still use the `⌘K` (or `Ctrl+K`) listener pattern from the [cmdk](https://cmdk.paco.me/) docs to open the palette globally.

## Examples

### Basic

A simple command menu in a dialog ([cmdk](https://cmdk.paco.me/) pattern).

<Preview>
  <CommandBasicDemo />
</Preview>

### Shortcuts

Items with `CommandShortcut` for keyboard hints.

<Preview>
  <CommandShortcutsDemo />
</Preview>

### Groups

Groups with icons and separators between sections.

<Preview>
  <CommandGroupsDemo />
</Preview>

### Scrollable

Scrollable list when there are many items.

<Preview>
  <CommandScrollableDemo />
</Preview>

### RTL

Right-to-left layout with translated labels.

<Preview>
  <CommandRtlDemo />
</Preview>

## API Reference

The Command component composes `Command`, `CommandInput`, `CommandList`, `CommandEmpty`, `CommandGroup`, `CommandItem`, `CommandSeparator`, `CommandShortcut`, and `CommandDialog`. See the [cmdk documentation](https://cmdk.pacocoursey.com/) for filtering, value, and keyboard behavior.
