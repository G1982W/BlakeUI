---
title: Context Menu
description: A menu that appears on right-click or long-press for contextual actions.
base: base
component: true
---

import { Preview } from "@/components/preview";
import Installation from "@/components/installation";
import registry from "@/registry.json";
import {
  ContextMenuBasicDemo,
  ContextMenuCheckboxDemo,
  ContextMenuDestructiveDemo,
  ContextMenuGroupsDemo,
  ContextMenuIconsDemo,
  ContextMenuRadioDemo,
  ContextMenuRtlDemo,
  ContextMenuShortcutsDemo,
  ContextMenuSubmenuDemo,
} from "@/components/ui/context-menu-demo";

## Example

<ComponentPreview name="context-menu">
  <ContextMenuBasicDemo />
</ComponentPreview>

## Installation

<Installation
  dependencies={["radix-ui", "lucide-react"]}
  code={registry["context-menu"]?.source ?? ""}
  fileName="/components/ui/context-menu.tsx"
/>

## Usage

```tsx
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuTrigger,
} from "@/components/ui/context-menu";
```

```tsx
<ContextMenu>
  <ContextMenuTrigger>Right click here</ContextMenuTrigger>
  <ContextMenuContent>
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

## Examples

### Basic

A simple context menu with a few actions ([shadcn Context Menu](https://ui.shadcn.com/docs/components/radix/context-menu) pattern).

<Preview>
  <ContextMenuBasicDemo />
</Preview>

### Submenu

Use `ContextMenuSub`, `ContextMenuSubTrigger`, and `ContextMenuSubContent` to nest secondary actions.

<Preview>
  <ContextMenuSubmenuDemo />
</Preview>

### Shortcuts

Add `ContextMenuShortcut` to show keyboard hints.

<Preview>
  <ContextMenuShortcutsDemo />
</Preview>

### Groups

Group related actions with `ContextMenuGroup` and separate them with `ContextMenuSeparator`.

<Preview>
  <ContextMenuGroupsDemo />
</Preview>

### Icons

Combine icons with labels for quick scanning.

<Preview>
  <ContextMenuIconsDemo />
</Preview>

### Checkboxes

Use `ContextMenuCheckboxItem` for toggles. Control checked state with `checked` and `onCheckedChange`.

<Preview>
  <ContextMenuCheckboxDemo />
</Preview>

### Radio

Use `ContextMenuRadioGroup` and `ContextMenuRadioItem` for exclusive choices.

<Preview>
  <ContextMenuRadioDemo />
</Preview>

### Destructive

Use `variant="destructive"` on `ContextMenuItem` for destructive actions.

<Preview>
  <ContextMenuDestructiveDemo />
</Preview>

### RTL

Right-to-left layout with translated labels. Wrap the trigger area in a container with `dir="rtl"`.

<Preview>
  <ContextMenuRtlDemo />
</Preview>

## API Reference

The Context Menu composes Radix primitives: `ContextMenu`, `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem`, `ContextMenuCheckboxItem`, `ContextMenuRadioItem`, `ContextMenuRadioGroup`, `ContextMenuLabel`, `ContextMenuSeparator`, `ContextMenuShortcut`, `ContextMenuGroup`, `ContextMenuSub`, `ContextMenuSubTrigger`, and `ContextMenuSubContent`. See the [Radix Context Menu documentation](https://www.radix-ui.com/primitives/docs/components/context-menu) for behavior and accessibility details.
