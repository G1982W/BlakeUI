---
title: Config Members 2
description: Members grouped by role (Admin, Member, Viewer) with avatars, names, and action menus. Invite dialog.
base: base
component: true
---

import { SettingsMembers4 } from "@/components/ui/settings-members-4";
import Installation from "@/components/installation";
import registry from "@/registry.json";

## Example

<ComponentPreview name="settings-members-4" premium showDevicePreview>
  <SettingsMembers4 />
</ComponentPreview>

## Introduction

**Config Members 2** organizes members by role:

- **Header**: “Members by role” and Invite button (dialog with email and role).
- **Sections**: One block per role (Admin, Member, Viewer) with a role badge and count. Under each, a list of members (avatar, name, email) with a three-dot menu (Change role, Remove).

Uses [Button](/docs/button), [Input](/docs/input), [Avatar](/docs/avatar), [Badge](/docs/badge), [Dropdown Menu](/docs/dropdown-menu), [Dialog](/docs/dialog), and [Native Select](/docs/native-select). Good when you want to browse or manage members by access level.

<PremiumOnly>

## Installation

<Installation
  dependencies={[]}
  code={registry["settings-members-4"]?.source ?? ""}
  fileName="/components/ui/settings-members-4.tsx"
/>

You need the Button, Input, Avatar, Badge, Dropdown Menu, Dialog, and Native Select components in your project.

</PremiumOnly>

## Usage

```tsx
import { SettingsMembers4 } from "@/components/ui/settings-members-4";

<SettingsMembers4 />;
```

Feed the component from your API grouped by role and connect invite and actions to your backend.
